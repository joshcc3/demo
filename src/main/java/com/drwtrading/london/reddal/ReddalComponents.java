package com.drwtrading.london.reddal;

public enum ReddalComponents {

    SELECT_IO_CLOSE,
    SELECT_IO_SELECT,
    SELECT_IO_UNHANDLED,

    INDY_HUB_AVAILABLE,
    INDY_UNDERLYING_TCP,
    INDY_TCP_PROTOCOL_FAILURE
}
