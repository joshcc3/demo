package com.drwtrading.london.reddal.orderManagement.remoteOrder.logging;

public enum NibblerRemoteModifyColumns {

    timestamp,

    USERNAME,
    CHAIN_ID,
    SYMBOL,
    SIDE,
    ORDER_TYPE,
    ALGO_TYPE,
    TAG,

    FROM_PRICE,
    FROM_QTY,

    TO_PRICE,
    TO_QTY
}
