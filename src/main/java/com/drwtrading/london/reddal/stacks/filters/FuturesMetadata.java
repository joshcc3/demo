package com.drwtrading.london.reddal.stacks.filters;

import java.util.Map;

public class FuturesMetadata extends Metadata.AMetadata {

    public FuturesMetadata(final String symbol, final Map<String, String> data) {
        super(symbol, data);
    }
}
