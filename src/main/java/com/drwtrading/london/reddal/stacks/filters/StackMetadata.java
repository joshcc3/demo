package com.drwtrading.london.reddal.stacks.filters;

import java.util.Map;

public class StackMetadata extends Metadata.AMetadata {

    public StackMetadata(final String symbol, final Map<String, String> data) {
        super(symbol, data);
    }
}
